# Comment Audit: limesium-reactive-logging - Round 2 (follow-up)

> **Status: ✅ REMEDIATED (2026-08-22).** Finding 22 is fixed in `1ee152ac`; the remediation table of the
> whole chain is section 8 of the round-1 report.

1. Codebase identification
   - **Repository:** `https://github.com/dhaase/tool-box.git`
   - **Commit hash:** `c5b5de0557591be2d246ab0907e1db290cf6530f` (full; unchanged since round 1 - no remediation has landed between the rounds)
   - **Reference (branch/tag):** `refs/heads/main`; tag: none identified
2. Scope of the analysis
   - **Included:** `./limesium-reactive-logging/src/main/kotlin/` and `./limesium-reactive-logging/src/main/resources/` (relative to the workdir) - unchanged from round 1
   - **Excluded:** unchanged from round 1 (`./limesium-reactive-logging/target/`, sibling modules as read-only counterparts, earlier analysis documents as read-only history)
   - **Test code in scope:** yes (`./limesium-reactive-logging/src/test/kotlin/`)
   - **Public API / internal boundary:** unchanged from round 1 (Kotlin visibility modifier; see the round-1 report)
3. Analysis environment & tools
   - **Target environment:** OpenJDK 21, Kotlin 2.4.10, Spring Boot 4.1.0 / Spring Framework 7.0.8
   - **Build system:** Apache Maven 3.9.15
   - **Documentation tool:** NONE configured; no `failOnWarning`; symbolic references unchecked (the reason this round exists)
   - **Convention sources:** unchanged from round 1
   - **Predecessor reports in the chain:** `./limesium-reactive-logging/docs/assessment/COMMENT_AUDIT-2026-08-22T22-11-51.md` (round 1); trigger: the sibling audit `./limesium-servlet-logging/docs/assessment/COMMENT_AUDIT-2026-08-22T22-18-09.md`, whose finding 1 and "cross-report note" in section 2 identified the omission
   - **Analysis tools used:** re-verification of every `[…]` target of round 1 by an exhaustive list (`grep -roE "\[[A-Za-z.]+\]"` over `src/main`, each target resolved by hand against the current declarations), plus the round-1 method for everything else
4. Placement & Output
   - **Working directory (workdir):** `/home/dirk/IdeaProjects/tool-box`
   - **Report output path:** `./limesium-reactive-logging/docs/assessment/COMMENT_AUDIT-2026-08-22T22-11-51.R2.md` (round suffix per the chaining rule; the round-1 file is untouched)
   - **Scope root (relative to the workdir):** `./limesium-reactive-logging/`
   - **Path convention for locations:** `<path relative to the workdir>:<line>`

## 1. Executive summary

This round corrects one omission of round 1 and leaves everything else standing. Round 1 reported "unresolved `[…]` references 0 (manual check, 40 distinct targets)". The manual check missed the reference `[checked]` in the class KDoc of `EndpointLogField` (`./limesium-reactive-logging/src/main/kotlin/eu/inqudium/limesium/reactive/logging/EndpointLogFields.kt:24`): the KDoc explains why the type-asserting helper "is named [checked] and not something suggesting a conversion" - the helper is named `format`; no declaration `checked` exists in the module. The sentence describes a renamed declaration and its link resolves to nothing, which is a Critical per the report's own rubric. The same defect exists in the servlet twin (its audit reports it as finding 1), so the sentence was drifted at the time of copying, not by later divergence.

The correction changes the round-1 statistics (Critical 2 → 3, total 21 → 22, 📚⚪ 6 → 7) and the mechanical inventory (unresolved references 0 → 1). It does not change the ranking (`EndpointLogFields.kt` was already score 5), the pilot recommendation, the pass structure, or the conclusion - except that the risk "unchecked symbolic references everywhere - today correct, tomorrow unknown" must now read "today one is already wrong". That strengthens, rather than alters, the round-1 guardrail of binding Dokka with `failOnWarning`.

**Conclusion (delta to round 1).**

1. *State:* unchanged - more knowledge than dead weight; the drift cluster gains one member (now 4 sites of copy/history residue: `five meters`, `requestDestroyed`, the Spring-MVC/destruction wording, `[checked]`).
2. *Reducibility:* unchanged (~8-10 % condensation, no decompositions).
3. *Essential risks:* the "unchecked references" risk is no longer hypothetical - 1 of 41 targets is broken at the analyzed commit.

## 2. Scope & methodology

Scope, boundaries, convention sources and churn basis are those of round 1 and are not repeated. Method of this round: the exhaustive reference list was regenerated and every target resolved against the declarations of the current commit; 41 distinct targets (round 1 counted 40 - `[checked]` was the one not in the list because the grep underlying the manual check had been read, not iterated). All other round-1 findings were re-read against the code; none has changed status, because no remediation commit has landed since.

**Known-findings register.** Round-1 findings 1-21 are carried forward unchanged with their IDs, severities and passes (see the round-1 report for the full text). New in this round: finding 22. Change log: statistics and inventory corrected (section 3); round-1 sentence "unresolved `[…]` references 0" superseded.

Known limitations: unchanged from round 1.

## 3. Statistics (corrected, whole chain)

| Severity | Round 1 | Round 2 |
|---|---:|---:|
| 🔴 Critical | 2 | **3** |
| 🟠 High | 3 | 3 |
| 🟡 Medium | 9 | 9 |
| 🟢 Low | 7 | 7 |
| **Total** | **21** | **22** |

📚 markers (chain): 📚🔴 0 · 📚🟠 6 · 📚🟡 9 · 📚⚪ **7**.

**Mechanical inventory correction:** unresolved `[…]` references **1** (was reported as 0); documentation on removed/renamed declarations **1** (was 0); distinct symbolic targets 41 (was 40). All other inventory figures of round 1 stand.

**Detected systemic patterns:** 4 (unchanged); pattern 1 (servlet-twin wording residue) gains the `[checked]` site as a further occurrence of "copied sentence not revisited".

## 4. File ranking table

Unchanged from round 1. `EndpointLogFields.kt` keeps score 5; its rationale now reads "unresolved `[checked]` (Critical); 60 % comment density; twin-copied KDoc already diverged; contract documentation for the index". Pilot recommendation unchanged (`ExchangeLogEmitter.kt` + `EndpointLoggingMetrics.kt`).

## 5. Findings

Findings 1-21: see round 1, unchanged. Ordered by residual risk, the new finding ranks first together with round-1 findings 1 and 2 (all three are statements that are wrong at the analyzed commit).

### 🔴 Critical

- [x] 22. [FILE:LINE — `./limesium-reactive-logging/src/main/kotlin/eu/inqudium/limesium/reactive/logging/EndpointLogFields.kt:24`] {Critical} {Confidence: high} {📚⚪} {drift & staleness / reference quality} The class KDoc says the type-asserting helper is "named [checked]" - the function is `format`, and the link resolves to nothing
  - Finding: "[format] converts nothing - every field goes on the wire exactly as supplied - it only GUARANTEES the type, which is why the helper is named [checked] and not something suggesting a conversion." No declaration `checked` exists in the file or the module. The sentence argues for a name the code does not carry, and `[checked]` is an unresolved symbolic reference that no tool reports because no documentation run is configured. Identical text in the servlet twin (`./limesium-servlet-logging/src/main/kotlin/eu/inqudium/limesium/servlet/logging/EndpointLogFields.kt:22`, finding 1 of that module's audit).
  - Information class: Why (naming rationale)
  - Drift test: yes - it drifted; the rename (or the naming decision) was never reflected in the rationale.
  - Actionability: no - the reader sees `format` and is told it is called `checked`.
  - Carrier strategy: name - either the function carries the name the sentence argues for, or the sentence goes; the remaining statement ("asserts the type, never converts") is already carried by `format`'s own KDoc in the enum body.
  - Residual information: none beyond `format`'s KDoc.
  - Target location: n/a
  - Pass: 1 (wording); a rename, if preferred, is pass 4 in both twins with `EndpointLogFieldTest` as the consequence check

## 6. Systemic patterns

Unchanged from round 1 (4 patterns). Pattern 1 - servlet-twin wording residue / copied sentences not revisited - now counts ~7 occurrences (was ~6), the `[checked]` site added; counting basis as in round 1 plus `grep -n "\[checked\]"`.

## 7. Implementation order & guardrails

Unchanged from round 1, with two additions:

- **Pass 1** gains finding 22 (wording correction, 📚⚪, mechanical).
- **Pass 5** - the proposal to bind Dokka with `failOnWarning = true` is no longer preventive but corrective: one of 41 symbolic targets is already broken at the analyzed commit. The abort criterion "unresolved documentation references → 0" currently stands at 1 and is measurable only once the documentation run exists; until then it is re-checked by hand per audit round, which this round demonstrates is fallible.

All other passes, constraints, PR-slicing rules, non-proposals and abort criteria of round 1 apply verbatim.
