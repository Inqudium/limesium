<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>eu.inqudium</groupId>
    <artifactId>limesium-benchmarks</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>limesium-benchmarks</name>
    <!-- JMH verification benchmarks for the PERF_ANALYSIS / BENCH_REPORT trail (docs/assessment
         of the module under test). DELIBERATELY a standalone module, not a child of the limesium
         reactor: a measurement session must not change the product build, and `mvn verify` at the
         repository root must stay byte-identical with and without this directory. Build with
         `mvn -f benchmarks/pom.xml package` after `mvn -pl <module> -am install` of the module
         under test. Java sources for the same reason as the fuzz tests: the JMH annotation
         processor works without kapt. -->
    <description>JMH benchmarks verifying the PERF_ANALYSIS findings of the limesium modules</description>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <jmh.version>1.37</jmh.version>
        <limesium.version>3.0.0</limesium.version>
        <uberjar.name>benchmarks</uberjar.name>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.openjdk.jmh</groupId>
            <artifactId>jmh-core</artifactId>
            <version>${jmh.version}</version>
        </dependency>
        <dependency>
            <groupId>org.openjdk.jmh</groupId>
            <artifactId>jmh-generator-annprocess</artifactId>
            <version>${jmh.version}</version>
            <scope>provided</scope>
        </dependency>
        <!-- The modules under test, from the local repository (see the build note above). -->
        <dependency>
            <groupId>eu.inqudium</groupId>
            <artifactId>limesium-reactive-logging</artifactId>
            <version>${limesium.version}</version>
        </dependency>
        <dependency>
            <groupId>eu.inqudium</groupId>
            <artifactId>limesium-servlet-logging</artifactId>
            <version>${limesium.version}</version>
        </dependency>
        <!-- The servlet module keeps the servlet API provided (the container's, in production);
             the benchmark JVM has no container, so the API and Spring's mock response come in
             explicitly for the writer-capture benchmark. -->
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <version>6.1.0</version>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-test</artifactId>
            <version>7.0.9</version>
        </dependency>
        <!-- Logback pinned into the benchmark JVM so the harness rule "root level OFF" (see
             src/main/resources/logback.xml) governs the default logging context instead of
             SLF4J's no-op or Logback's zero-config DEBUG default. -->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>1.5.20</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- JDK >= 23 disables annotation processing by default; JMH's BenchmarkList is
                 generated by an annotation processor, so it is re-enabled explicitly. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.1</version>
                <configuration>
                    <proc>full</proc>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.6.1</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <finalName>${uberjar.name}</finalName>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>org.openjdk.jmh.Main</mainClass>
                                </transformer>
                            </transformers>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
